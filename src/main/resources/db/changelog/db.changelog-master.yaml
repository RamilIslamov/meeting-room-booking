# Master changelog. Table migrations (users, rooms, bookings) are added in step 3.
# Each new changeset goes into db/changelog/changes/ and is referenced here, e.g.:
#   - include:
#       file: db/changelog/changes/001-create-users.yaml
databaseChangeLog: []
